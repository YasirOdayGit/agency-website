import React from 'react';
import ReactDOM from 'react-dom/client';

import { App } from './app';
import './index.css';

// @ts-ignore
ReactDOM.createRoot(window.app).render(
    <React.StrictMode>
        <App />
    </React.StrictMode>
);
